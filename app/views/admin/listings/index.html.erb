<%= render 'admin/admin_index' %>

<% @listings.each do |listing| %>
  <table>
  <% listing.attributes.each do |key, value| %>
    <tr>
      <td><%= key %></td>
      <td><%= value %></td>
    </tr>
  <% end %>
  </table>
  <br>
  <hr>
<% end %>
